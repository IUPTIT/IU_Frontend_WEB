# IU Club Frontend — copy thành .env
# URL API backend (kèm prefix /api/v1)
VITE_API_URL=http://localhost:3456/api/v1

# Cổng dev server (npm run dev) — mặc định 5173
DEV_PORT=5173
# Cổng preview/production (npm run start) — mặc định 6666
WEB_PORT=6666
